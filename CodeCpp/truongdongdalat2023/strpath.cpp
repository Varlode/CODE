#include <bits/stdc++.h>
using namespace std;

const int N = 200;
int n, m, D;    
char a[N][N];
string s;

int main() {
    ios_base::sync_with_stdio(0); cin.tie(0); cout.tie(0);
    cin >> n >> m >> D;
    cin >> s;
    for (int i = 1; i <= n; i++) for (int j = 1; j <= n; j++) 
        cin >> a[i][j];   
}