#include<bits/stdc++.h>
using namespace std;

const int N = 1e5;
int n;
int t[N], l1[N], l2[N], r1[N], r2[N];

int main() {
    cin >> n;
    for (int i = 1; i <= n; i++) {
        cin >> t[i] >> l1[i] >> l2[i] >> r1[i] >> r2[i];
    }
}