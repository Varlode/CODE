#include<bits/stdc++.h>
using namespace std;

const int maxN = 1e4+9;
int n, d[maxN], c[maxN], L, P;

int main() {
    int T; cin >> T;
    while (T--) {
        cin >> n;
        for (int i = 1; i <= n; i++) {
            cin >> d[i] >> c[i];
        }
        cin >> L >> P;
    }
}